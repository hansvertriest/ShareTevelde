import { NextFunction, Request, Response } from 'express';

class PostController {
  public uploadPost = async (req: Request, res: Response, next: NextFunction): Promise<void> => {
    // if 
  }
}

export default PostController;
